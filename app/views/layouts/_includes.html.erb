
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %> 
	<script type="text/javascript">
		<% if user_signed_in? %>
		Pusher_config.init(
			<%= current_user.id %>,
			"<%= Pusher.key %>" );
		<% end %>
	</script>   
	