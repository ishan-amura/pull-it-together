<div class="container">
<div class="card">
		<div class="card-content">
			<div class="card-title">
				<%= @post.title %>
			</div>
			<div class="card-title right">
				<%= @post.user %>
			</div>
			<%= @post.body %>
			<div class="divider"></div>
			<%= render 'comments/index', post:@post%>
		</div>
		<div class="card-action">
			<%= link_to(follow_post_path(@post),method: :post) do%>
						follow
			<% end %>
		</div>
</div>
</div>